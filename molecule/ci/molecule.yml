---
dependency:
  name: galaxy
driver:
  name: default
platforms:
  - name: ubuntu-test
provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
    verify: verify.yml
verifier:
  name: ansible
